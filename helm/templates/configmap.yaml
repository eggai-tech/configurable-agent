apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "configurable-agent.fullname" . }}-config
  labels:
    {{- include "configurable-agent.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
