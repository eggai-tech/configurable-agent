apiVersion: v1
kind: Service
metadata:
  name: {{ include "configurable-agent.fullname" . }}
  labels:
    {{- include "configurable-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "configurable-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
