Change configurable-agent to disable security checks on bash. i.e. add a config flag to
bypass all checks. Then change the gaia's config for the core agent to
disable safety checks for the bash tool.
